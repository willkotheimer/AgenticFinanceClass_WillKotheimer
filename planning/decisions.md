# Decisions log

One entry per decision. Never edit an entry; supersede it with a new one.

| ID | Date | Decision | Why |
|---|---|---|---|
| D-001 | 2026-09-13 | Python, FastAPI, Docker. No .NET. | Course requirement. |
| D-002 | 2026-09-13 | The FastAPI service does **not** depend on StockAggregator (no calls to its API or database). It may reuse the same Yahoo Finance chart endpoint via `yfinance`. | Keeps the capstone self-contained and runnable by graders; StockAggregator integration is a follow-up. |
| D-003 | 2026-09-13 | The project's centre of gravity is *research* agents (reading filings, news, structured financials), not trade execution. | Matches the professor's emphasis. |
| D-004 | 2026-09-13 | Selected milestones are demonstrated in Jupyter notebooks that import from `app/` and are also executed headless in CI. | Human-verifiable checkpoints keep agent-driven development honest; matches the course format. |
| D-005 | 2026-09-13 | Two test layers: deterministic pytest (no LLM) and scored evals (LLM in the loop) with regression thresholds. | Evals are a grading criterion; deterministic tests keep the harness itself trustworthy. |
| D-006 | 2026-09-13 | Working files: `ideas.md` (log), `decisions.md` (this), `final_idea.md` (chosen project), `alternate_followup_ideas.md` (everything else), `governance.md` (written after the final idea is fixed). | Agreed layout. |
| D-007 | 2026-09-13 | The capstone spec in `capstone_requirements.md` is the source of truth; the first deliverable is the **Part 1 proposal** (1–2 pages, diagram, component mapping, no code), due about two weeks after the course ended. `final_idea.md` is written as that proposal. | Part 1 is reviewed before we may build; the four build weeks start only after feedback. |
| D-008 | 2026-09-13 | Target **five** of the eight components: C1 retrieval & grounding, C5 tool-using workflow, C6 SKILL.md, C7 evaluation & tracing, C8 security. C2 (tables) and C4 (market data) are stretch goals if they fall out naturally. | Three is the minimum; five covers every grading criterion without forcing audio/charts we have no data for. |
| D-009 | 2026-09-13 | Submit as a private GitHub repo with the instructor added as collaborator. | Course submission instructions. |
| D-010 | 2026-09-13 | Adopt the course's "Alpha" maturity stack: Dockerized app + FastAPI + tracing, with SSE for long-running runs; Streamlit (or equivalent) as the only thing the browser talks to. | Matches D-001 and the professor's own maturity model; scores on G1/G5/G6. |
| D-011 | 2026-09-13 | Structured outputs use tiers 1 and 2 (strict schema + Pydantic validate/retry), labels normalised to small enums, raw output always logged. | Course guidance on structured-output tiers; directly gradable under G4/G5. |
| D-012 | 2026-09-13 | Every run is bounded by declared termination conditions, a turn/cost circuit breaker, and a budget written in `governance.md`. | Grading criterion G7 asks for "a budget somebody chose". |
| D-013 | 2026-09-13 | The system is **ticker-agnostic** (any SEC registrant). Evaluation uses a fixed **small-cap roster of 9**, verified on EDGAR: BFLY, KDK, POET, LGVN, RXRX, LUNR, BARK, SOUN, MNTS. POET (Canadian, 20-F/6-K, IFRS XBRL) is added **last** as the deliberate foreign-filer edge case. Roster coverage is a stated non-goal; ~3–5 hand-verified questions per company (~30–40 golden cases). | Scope is graded by eval cases, not breadth. Small caps defeat model memorisation so the citation check actually tests retrieval; they also carry the interesting questions (going concern, dilution, reverse splits, listing deficiencies, cash runway). None overlap StockAggregator's 64. |
| D-014 | 2026-09-13 | Target maturity stage (course maturity model) is **Alpha** — Dockerized + FastAPI + tracing + eval datasets — plus two Hardening items: a tested fallback model and HITL. **Non-goals, stated in the design doc:** real users, auth, alerts (Production); Kubernetes / Ray Serve / vLLM / autoscaling (Scale). | The rubric grades harness, evals, security and documentation, not deployment stage or user count; naming non-goals is itself graded (G8, "may and may not do"). |
| D-015 | 2026-09-13 | **Delivery order: notebook → FastAPI → React.** The graded core runs end-to-end from notebooks that import `app/`; FastAPI (`POST /runs`, SSE `GET /runs/{id}/events`) is a thin layer added after the notebooks pass; a React SPA (not Streamlit) is last and optional. Each notebook section names the `app/` module it exercises and the API route it will become. | Part 3 accepts "a notebook or repository", so the notebook alone is submittable; the API and UI are additive and can be dropped if the build window gets tight. React matches StockAggregator.UI for the later port. Supersedes the Streamlit choice in D-010. |
| D-016 | 2026-09-13 | **Framework-native over hand-rolled.** Use LangGraph `StateGraph` nodes/edges/reducers, Pydantic models for all state and outputs, typed `@tool` functions with declared schemas, and `interrupt()` for HITL — no ad-hoc loops, dict-shaped state, or string-parsed outputs. | Makes the notebook a readable spec of behaviour that maps 1:1 to `app/` modules and API routes; it is also the concrete difference between "a harness around the model" and "a chain of calls" (G1). |
| D-017 | 2026-09-13 | **Notebooks are governance contracts.** Every rule in `governance.md` is paired with an executable notebook assertion that demonstrates the rule being enforced (budget breach halts, injection blocked, out-of-allowlist tool denied, extrapolation refused). The Pydantic DTOs are the contract for *shape*; the tested notebooks are the contract for *behaviour*. Both run in CI; breaking either is a contract break. | G6 grades boundaries "present **and enforced**" — a document proves presence, only a run proves enforcement. A rule with no assertion is a claim; a rule with one has been run. |
| D-018 | 2026-09-13 | **Proposal and design-doc style: ASD-STE100 Simplified Technical English.** Active voice, present tense, one idea per sentence, ≤ 20 words per sentence, one term per concept, lists over prose, at most one example per section and only where needed. Written for an expert reader: no definitions of course concepts. Complexity is allowed only where the system is complex (the multi-agent section). Gherkin scenarios are permitted when asked for and are exempt from the example limit. | Reviewer is an expert with many submissions to read; clarity is graded (G8). |
| D-019 | 2026-09-13 | **UI: Streamlit calling `app/` in-process. FastAPI is dropped from the capstone** and moves to the StockAggregator follow-up. Delivery order is notebooks → Streamlit. The graph exposes one entry point, `app.run(request) -> event stream`; Streamlit (and any later API) is a thin caller of it. Supersedes the FastAPI and React parts of D-001, D-010, D-015. | Streamlit renders server-side, so the browser never sees keys or code — that is the zero-trust UI boundary the course describes. FastAPI adds a second service without adding rubric coverage. Fewer moving parts in a four-week build. |
| D-020 | 2026-09-13 | **No UI in the capstone.** Notebooks are the interface: input, run, memo, trace link, eval scores. `app.run(request) -> event stream` stays the single entry point. Docker compose carries only Langfuse, Postgres, and a Python image that runs pytest and papermill. Supersedes the Streamlit part of D-019; FastAPI and any UI remain follow-ups. | Part 3 accepts a notebook. A UI is scope with no rubric return, and Streamlit is a tool I would not use again. |
| D-021 | 2026-09-13 | **Domain: P&C insurance.** The eval roster is nine small-cap insurers verified on EDGAR (all SIC 6331, all 10-K filers): ROOT, LMND, HIPO, KINS, HCI, HRTG, UFCS, KNSL, NODK. Question templates become insurance metrics: loss development triangles (chain-ladder, C2), combined ratio, reserve development, reinsurance recoverables, cat losses, listing / charter events. The C4 Monte Carlo tool becomes reserve variability (bootstrap the triangle). The harness, evals, and security design are unchanged. Supersedes the roster in D-013; the generic small-cap roster moves to the follow-ups. | Professor allows own-domain projects; insurers are SEC filers with the most table-heavy 10-Ks, so C1/C2 ground truth stays deterministic. Domain knowledge makes my pushback on results sharper, and insurtech knowledge is career-relevant. |
| D-022 | 2026-09-13 | **Revert to the generic small-cap roster** (D-013): BFLY, KDK, POET, LGVN, RXRX, LUNR, BARK, SOUN, MNTS, with the cash-runway / dilution / going-concern templates. The C4 Monte Carlo tool is the runway / dilution simulation. The P&C insurer roster, templates, and primer move to the follow-ups. Supersedes D-021. | I can judge whether the agent is right about a small cap today; judging it on reserve development would require learning the domain during the build, which weakens the pushback the evals depend on (D-017). |
| D-023 | 2026-09-18 | **Test strategy is fixed before code** (`test_strategy.md`): deterministic oracles (XBRL, submissions index, pinned passages, fixed seed) over an LLM judge; baselines beside every score (no-retrieval, trivial, single-agent ablation); negative controls (wrong filings, unanswerable, poisoned, extrapolation); metamorphic, discrimination pairs, and stability runs. Golden cases are written from EDGAR before the agents exist. One ticker per run; the roster lives only in eval sweeps. | "Working" must be distinguishable from "doing things." A score without a floor and an unfakeable oracle is a claim. Cases derived from agent runs would grade the agent against itself. |
| D-024 | 2026-09-18 | **Filings only. No price data, no news.** `yfinance` is dropped; EDGAR is the sole outbound source. The system reads and interprets filings; it makes no claim about price or market reaction. C4 is covered by the Monte Carlo tool over XBRL cash-flow history. Price/news correlation and the earnings-reaction desk move to the follow-ups. | Fewer assumptions in the current regime; every claim has a document behind it; one outbound source means one injection surface and one approval-gate job. Nothing in G1–G8 rewards market prediction. |
| D-025 | 2026-09-18 | **Final idea: Plan A, Filings Research Desk, filings only.** Given a ticker and a question, a governed multi-agent system reads the company's SEC filings and XBRL facts from EDGAR and returns a research memo in which every figure is traceable to a source. No price data, no news, no UI, no trade execution, no model-generated code. Components C1, C2, C5, C6, C7, C8, with C4 via the Monte Carlo tool over XBRL cash flows. `final_idea.md` is now the Part 1 proposal. | Signed off. Plans B and C stay in `plans.md` as considered alternatives; C is the fallback if the build window gets tight. |
| D-026 | 2026-09-18 | **Repo is public**: `github.com/willkotheimer/AgenticFinanceClass_WillKotheimer`. Submission is the link; no collaborator step. Secrets only via environment variables; a pre-commit secret scan ships with the first code PR. Supersedes D-009. | It is a class project, not IP. Public removes the collaborator step and makes the work citable. |
| D-027 | 2026-09-18 | **Simplest architecture that meets the rubric.** Every component must earn its place; when two designs score the same, the one with fewer parts wins. | Fewer parts: fewer failure modes, less to test, less to explain. G8 rewards clarity. |
| D-028 | 2026-09-18 | **All writing is STE and outline-shaped** — extends D-018 from the proposal to every document. Rules: one idea per line; high-level concepts; flowchart arrows (`A → B → C`) where sequence matters; a concept may be shown two ways (arrow line + one sentence) but never in more than one paragraph; long prose only for the one main description of the idea. Instructor repos cloned to `repos/` for reference: `harness_engineering`, `ai-agents-the-definitive-guide`, `SkillOpt`, `AgensFlow`, `ORM-self-improving-ai-agents-course`. | Outline form is faster to read, faster to check, and harder to hide vagueness in. |
| D-029 | 2026-09-18 | **Keep the kernel, not the implementation.** The kernel of this project is *a governed system reads filings and returns a memo whose every figure is traceable to a source*. Libraries, topology, and tools may change; the kernel may not. The instructor's code may be used or adapted, with attribution; we take its concepts first and its code where it fits. | A project defined by its kernel survives a library swap; one defined by its implementation does not. The instructor offered her code; refusing it would be a constraint with no benefit. |
| D-030 | 2026-09-18 | **Four roles, not six**: supervisor, researcher, writer, reviewer, plus the human. Each role exists because it holds a different context boundary. The writer never sees raw filing text; the reviewer is the only role that reads XBRL; the supervisor holds no tools. Three read-only tools, all EDGAR. No memory across runs. Three declared exits: accept, escalate, halt. Recorded in `proposal.md`. | D-027: fewer parts. The skeptic and judge merge into one reviewer with a verdict enum; the ablation "with vs. without reviewer" still tests whether review earns its cost. The writer's boundary makes injection defence structural, not a filter. No cross-run memory removes the memory-poisoning surface entirely. |

## Open questions (not yet decisions)


- Exact Part 1 due date — confirm with the professor (assumed ~2026-09-27).
- Model provider and orchestration library (course uses LangGraph; provider varies by notebook).
- Observability backend: Langfuse (self-hostable in docker-compose) vs LangSmith (hosted).
- Which security tooling is realistic locally: LlamaFirewall, a custom scanner, or both.
